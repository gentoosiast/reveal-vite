{
  "name": "reveal-vite",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "format": "prettier --write .",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist -e presentation"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "prettier": "^3.0.0",
    "vite": "^5.1.5"
  },
  "dependencies": {
    "reveal.js": "^5.0.5"
  }
}
