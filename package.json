{
  "name": "reveal-vite",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist -e presentation"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "prettier": "^2.8.8",
    "reveal.js": "^4.5.0",
    "vite": "^4.3.9"
  }
}
