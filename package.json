{
  "name": "reveal-vite",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "format": "prettier --write .",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist -e presentation"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.5.3",
    "vite": "^6.3.2"
  },
  "dependencies": {
    "reveal.js": "^5.2.1"
  }
}
